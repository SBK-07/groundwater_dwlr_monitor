name: groundwater_monitor
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations: # Required for multilingual support
    sdk: flutter
  intl: ^0.20.2           # ✅ upgraded to match Flutter SDK
  cupertino_icons: ^1.0.8
  google_fonts: ^6.3.1
  fl_chart: ^1.1.1
  vector_math: ^2.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  generate: true
  assets:
    - assets/images/home_bg.jpg
